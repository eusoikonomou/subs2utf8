# utf8-encoding-converter
A tool that changes a file's encoding to UTF-8

Start the program from the directory that includes the file(s) that need encoding transformation and it will automatically change all file encodings to UTF-8.
